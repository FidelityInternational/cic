#!/usr/bin/env ruby

current_dir = Dir.pwd
command = <<~COMMAND
  docker run \
  --network bridge \
  -v #{current_dir}:/mnt/workdir \
  -w /mnt/workdir \
  lvlup/ci_course \
  ansible-doc #{ARGV.join(' ')}
COMMAND
system(command)
